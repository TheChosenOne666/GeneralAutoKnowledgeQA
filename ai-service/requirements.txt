# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# LangChain
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-openai==0.3.0

# AI SDK
openai==1.59.3
httpx==0.28.1

# Document Processing
unstructured==0.16.13
pymupdf==1.25.2
# pdfplumber：PyMuPDF 不可用时的纯 Python 降级方案（部分 Windows 上 _mupdf.pyd 因 DLL 加载失败）
pdfplumber==0.11.9
python-docx==1.1.2
markdown==3.7

# Vector DB
pymilvus==2.5.3
asyncpg==0.31.0

# Utils
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
loguru==0.7.3
numpy==2.2.1

# Redis（与 Java 后端共用同一实例，实现 L1 检索结果 / L2 嵌入向量 / L3 会话缓存）
redis==5.2.1

# Elasticsearch（M5-8 复合检索 + 未来搜索索引）：工业级 BM25 关键词召回。
# 客户端锁 8.x（兼容 Python 3.13），当前连接本地 Elasticsearch 7.17
# （D:\Elasticsearch\elasticsearch-7.17.23，含 IK 中文分词），通过「兼容模式」header
# （config.elasticsearch_compat_7=True）与之通信，无需降级客户端。若改连 8.x 服务器，
# 将 elasticsearch_compat_7 设为 false 即可走原生协议。
elasticsearch==8.17.0
